# CPU 사용률 최적화

## 🔍 문제 분석

### CPU 80% 사용 원인

1. **각 메시지마다 print 출력** (가장 큰 원인)
   - `services/recommender.py` 625-627줄
   - 매 메시지마다 콘솔에 출력 → I/O 오버헤드 발생
   - 초당 10만건 처리 시 10만번의 print 호출

2. **각 메시지마다 DB 커밋**
   - `services/recommender.py` 295줄: `self.db_session.commit()`
   - 각 메시지마다 개별 커밋 → I/O 대기 시간 증가

3. **batch_size 증가**
   - 이전: 50 또는 100
   - 현재: 200
   - 배치가 커져도 각 메시지마다 개별 처리

---

## ✅ 적용된 최적화

### 1. print 문 제거
- **파일**: `services/recommender.py`
- **변경**: 메시지마다 출력하던 print 문 주석 처리
- **효과**: I/O 오버헤드 대폭 감소

### 2. batch_size 조정
- **파일**: `config.yml`
- **변경**: 200 → 100
- **효과**: 메모리 사용량 감소, CPU 부하 분산

---

## 📊 예상 효과

### CPU 사용률
- **이전**: 80%
- **예상**: 40-50% (print 제거로 약 30-40% 감소)

### 처리량
- **변화 없음**: 메시지 처리 로직은 동일
- **안정성 향상**: CPU 여유로 인한 처리 안정성 증가

---

## 🔄 추가 최적화 가능 사항

### 1. 배치 DB 커밋 (향후 개선)
- 현재: 각 메시지마다 커밋
- 개선: 배치 단위로 모아서 커밋
- **주의**: 실패 시 재처리 로직 필요

### 2. 비동기 처리 (향후 개선)
- MongoDB 쓰기를 비동기로 처리
- **주의**: 복잡도 증가, 에러 처리 필요

### 3. 로깅 레벨 조정
- DEBUG 레벨 로그 제거 (이미 적용됨)
- INFO 레벨도 필요한 경우만 출력

---

## 🚀 적용 방법

1. Consumer 재시작 필요:
   ```bash
   docker restart recommend
   ```

2. 변경 사항 확인:
   - print 문 제거 확인
   - batch_size 100 확인
   - CPU 사용률 모니터링

---

## 📝 참고

- 이전 CPU 40%: batch_size가 작고 print 출력이 적었을 가능성
- 현재 CPU 80%: batch_size 증가 + 매 메시지 print 출력
- 최적화 후: print 제거로 CPU 40-50% 예상

