# Grafana Consumer 대시보드 가이드

## 📊 대시보드 패널 설명

### 1. Kafka Consumer 처리 통계 (누적)

**의미**: Consumer가 시작된 이후부터 지금까지 처리한 메시지의 **누적 합계**입니다.

**예시**:
- `51.0 K` = 51,000개의 메시지를 성공적으로 처리함
- 시간이 지나면서 계속 증가하는 그래프 = 메시지가 계속 처리되고 있음

**해석**:
- 그래프가 **위로 올라가는** = 정상 (메시지 처리 중)
- 그래프가 **평평한** = 메시지가 들어오지 않거나 Consumer가 멈춤
- 여러 선이 보이는 이유 = 파티션별(partition 0, 1, 2) 또는 이벤트 타입별로 구분

---

### 2. Kafka Consumer 처리률 (초당)

**의미**: **1초에 몇 개의 메시지를 처리하는지** 보여줍니다.

**예시**:
- `100 msg/s` = 초당 100개 메시지 처리
- `50 msg/s` = 초당 50개 메시지 처리

**해석**:
- 높은 값 = 처리량이 많음 (좋음)
- 낮은 값 = 처리량이 적음 (메시지가 적거나 처리 속도가 느림)
- 0 = 메시지가 없거나 Consumer가 멈춤

---

### 3. 처리 실패율

**의미**: 전체 처리 중 **몇 %가 실패했는지** 보여줍니다.

**예시**:
- `0%` = 모든 메시지가 성공적으로 처리됨 (좋음)
- `1%` = 100개 중 1개가 실패
- `5%` = 100개 중 5개가 실패 (주의 필요)

**해석**:
- **0% (녹색)** = 정상
- **1-5% (노란색)** = 주의 필요
- **5% 이상 (빨간색)** = 문제 발생, 확인 필요

---

### 4. 총 성공 처리 수

**의미**: 지금까지 **성공적으로 처리한 메시지의 총 개수**입니다.

**예시**:
- `51.0 K` = 51,000개 메시지 성공 처리
- 여러 숫자가 보이는 이유 = 파티션별 또는 이벤트 타입별로 구분

---

### 5. 총 실패 처리 수

**의미**: 지금까지 **실패한 메시지의 총 개수**입니다.

**예시**:
- `0` = 실패한 메시지가 없음 (좋음)
- `10` = 10개 메시지가 실패함

**해석**:
- **0** = 정상 (모든 메시지가 성공)
- **0보다 큰 값** = 일부 메시지가 실패함 (원인 확인 필요)

**현재 "nodata"인 이유**:
- 실패한 메시지가 없어서 메트릭이 생성되지 않음
- 또는 실패 메트릭이 기록되지 않음 (코드 수정 완료)

---

### 6. 총 에러 발생 수

**의미**: Consumer에서 발생한 **에러의 총 개수**입니다.

**에러 타입**:
- `json_decode_error`: JSON 파싱 실패
- `kafka_error`: Kafka 연결/통신 에러
- `processing_error`: 메시지 처리 중 에러
- `commit_error`: 오프셋 커밋 실패

**해석**:
- **0** = 에러가 없음 (좋음)
- **0보다 큰 값** = 에러가 발생함 (로그 확인 필요)

**현재 "nodata"인 이유**:
- 에러가 발생하지 않아서 메트릭이 생성되지 않음
- 또는 에러 메트릭이 기록되지 않음

---

### 7. Kafka Consumer 지연 시간 (Latency)

**의미**: Producer가 메시지를 보낸 시점부터 Consumer가 처리하기까지의 **시간**입니다.

**예시**:
- `P50: 100ms` = 50%의 메시지가 100ms 이내에 처리됨
- `P95: 500ms` = 95%의 메시지가 500ms 이내에 처리됨
- `P99: 1000ms` = 99%의 메시지가 1초 이내에 처리됨

**해석**:
- **낮은 값 (100ms 이하)** = 빠른 처리 (좋음)
- **중간 값 (100-500ms)** = 정상
- **높은 값 (1초 이상)** = 처리 속도가 느림 (주의 필요)

---

### 8. 에러 타입별 통계

**의미**: 어떤 종류의 에러가 몇 번 발생했는지 보여줍니다.

**에러 타입**:
- `json_decode_error`: JSON 파싱 실패
- `kafka_error`: Kafka 에러
- `processing_error`: 처리 중 에러
- `commit_error`: 커밋 에러

**해석**:
- 그래프가 **평평한 (0)** = 에러 없음 (좋음)
- 그래프가 **올라가는** = 에러 발생 (원인 확인 필요)

**현재 "nodata"인 이유**:
- 에러가 발생하지 않아서 메트릭이 생성되지 않음

---

### 9. 파티션별 처리된 메시지 수

**의미**: 각 파티션(Partition 0, 1, 2)에서 처리한 메시지 수입니다.

**예시**:
- `Partition 0: 736,830` = 파티션 0에서 736,830개 처리
- `Partition 1: 704,900` = 파티션 1에서 704,900개 처리

**해석**:
- 각 파티션의 값이 **비슷한** = 부하가 균등하게 분산됨 (좋음)
- 한 파티션만 **높은** = 부하가 한쪽으로 치우침

---

### 10. 파티션별 처리률 (초당)

**의미**: 각 파티션에서 **초당 몇 개의 메시지를 처리하는지** 보여줍니다.

**예시**:
- `Partition 0: 50 msg/s` = 파티션 0에서 초당 50개 처리
- `Partition 1: 45 msg/s` = 파티션 1에서 초당 45개 처리

**해석**:
- 각 파티션의 처리률이 **비슷한** = 균등한 처리 (좋음)
- 한 파티션만 **높은** = 부하가 한쪽으로 치우침

---

## ❓ "nodata"가 표시되는 이유

### 1. 총 실패 처리 수 - nodata

**원인**:
- 실패한 메시지가 없어서 `kafka_consumer_messages_total{status="failure"}` 메트릭이 생성되지 않음
- 또는 실패 메트릭이 기록되지 않음

**해결**:
- 코드 수정 완료: 실패 메시지도 메트릭에 기록되도록 수정됨
- 실패가 발생하면 자동으로 표시됨

### 2. 총 에러 발생 수 - nodata

**원인**:
- 에러가 발생하지 않아서 `kafka_consumer_errors_total` 메트릭이 생성되지 않음

**해결**:
- 에러가 발생하면 자동으로 표시됨
- 에러가 없는 것은 **정상**입니다

### 3. 에러 타입별 통계 - nodata

**원인**:
- 에러가 발생하지 않아서 메트릭이 생성되지 않음

**해결**:
- 에러가 발생하면 자동으로 표시됨
- 에러가 없는 것은 **정상**입니다

---

## ✅ 정상 상태 확인 방법

### 정상 상태:
1. ✅ **처리 통계 (누적)**: 그래프가 계속 올라감
2. ✅ **처리률 (초당)**: 0보다 큰 값 (예: 50-100 msg/s)
3. ✅ **처리 실패율**: 0% (녹색)
4. ✅ **총 성공 처리 수**: 계속 증가
5. ✅ **지연 시간**: 낮은 값 (100ms 이하)

### 문제 상태:
1. ❌ **처리 통계**: 그래프가 평평함 (메시지가 없음)
2. ❌ **처리률**: 0 (Consumer가 멈춤)
3. ❌ **처리 실패율**: 5% 이상 (빨간색)
4. ❌ **지연 시간**: 1초 이상 (처리 속도 느림)

---

## 🔧 문제 해결

### 실패/에러 메트릭이 표시되지 않는 경우:

1. **Consumer 재시작**:
   ```bash
   docker restart recommend
   docker exec recommend python -m services.kafka.kafka_consumer
   ```

2. **메트릭 확인**:
   ```bash
   curl http://localhost:9000/metrics | grep kafka_consumer
   ```

3. **Prometheus 타겟 확인**:
   - http://localhost:9090/targets
   - `kafka-consumer` 타겟이 UP 상태인지 확인

---

## 📝 요약

- **"nodata"는 에러가 아니라 에러가 없어서 표시되지 않는 것**입니다
- 실패/에러가 발생하면 자동으로 표시됩니다
- 코드 수정으로 실패 메트릭도 기록되도록 개선되었습니다

